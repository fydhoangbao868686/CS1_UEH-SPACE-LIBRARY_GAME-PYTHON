# UEH Library Escape - CS1 Project

Game 2D làm bằng **Python + Pygame**, gồm nhiều scene liên tiếp (Title → nhập ID → cutscene video → các thử thách nhập đáp án / click object → DONE popup → Final).

---

## 1) Requirements (cài pip để chạy được)

### Python version
- Khuyến nghị: **Python 3.10+** (3.11 cũng ok)

### Cài thư viện
Chạy lệnh sau trong terminal (ở thư mục project):

```bash
pip install pygame openpyxl ffpyplayer
